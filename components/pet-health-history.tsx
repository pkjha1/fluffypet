"use client"

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function PetHealthHistory() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Pet Health History</CardTitle>
      </CardHeader>
      <CardContent>{/* Health history implementation */}</CardContent>
    </Card>
  )
}

