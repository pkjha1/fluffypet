"use client"

import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"

export function ActiveMissions() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Active Missions</CardTitle>
      </CardHeader>
      <CardContent>{/* Active missions implementation */}</CardContent>
    </Card>
  )
}

