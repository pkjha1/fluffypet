import React from 'react';

const AdminPage = () => {
    return (
        <div className="p-4">
            <h1 className="text-2xl font-bold">Admin Dashboard</h1>
            <p className="mt-2">Manage services, veterinarians, places, and user accounts from this interface.</p>
            {/* Add components for managing services, vets, places, and users here */}
        </div>
    );
};

export default AdminPage;